To execute the code.

Step 1: Copy source_code.cc to scratch folder in ns3.

Step 2: Run the command "./waf --run scratch/source_code"

Step 3: After successful completion of above command 12 graphs(.plt) files would be generated in ns3  folder. Now use command "gnuplot <plotfile_name>".
Step 4: All Required plots will get generated.
